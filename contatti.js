const supabaseClient=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);const message=document.getElementById('message'),list=document.getElementById('contacts-list'),empty=document.getElementById('empty-state');async function load(){const{data:s}=await supabaseClient.auth.getSession();if(!s.session){location.href='login.html';return;}const{data,error}=await supabaseClient.rpc('get_my_contacts');if(error){message.textContent='Impossibile caricare i contatti.';return;}list.replaceChildren(...(data||[]).map(c=>{const a=document.createElement('a');a.className='contacts-row';a.href=`contatto.html?contact_id=${encodeURIComponent(c.id)}`;a.textContent=[c.first_name,c.last_name].filter(Boolean).join(' ')||'Contatto';return a;}));empty.hidden=(data||[]).length>0;message.textContent='';}load();
