const supabaseClient=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);const message=document.getElementById('message'),form=document.getElementById('area-form'),save=document.getElementById('save-button'),deleteSection=document.getElementById('delete-area-section'),deleteDialog=document.getElementById('delete-area-dialog'),deleteForm=document.getElementById('delete-area-form'),deleteInput=document.getElementById('delete-area-confirmation'),deleteConfirm=document.getElementById('confirm-delete-area'),deleteMessage=document.getElementById('delete-area-message');let areaId,areaName='',areaDescription=null,canDelete=false;const back=()=>location.href=`area.html?area_id=${encodeURIComponent(areaId)}`;const closeDeleteDialog=()=>deleteDialog.close();const updateDeleteConfirmationState=()=>deleteConfirm.disabled=deleteInput.value!==areaName;
async function load(){const{data:s}=await supabaseClient.auth.getSession();if(!s.session){location.href='login.html';return;}areaId=new URLSearchParams(location.search).get('area_id');if(!areaId){message.textContent='Area non specificata.';return;}document.getElementById('back-link').href=`area.html?area_id=${encodeURIComponent(areaId)}`;const[{data:area,error},{data:areas,error:rolesError}]=await Promise.all([supabaseClient.rpc('get_area',{p_area_id:areaId}),supabaseClient.rpc('get_my_areas')]);const role=(areas||[]).find(x=>x.id===areaId)?.role;if(error||rolesError||!area||!['owner','admin'].includes(role)){message.textContent='Non sei autorizzato a modificare questa Area.';return;}areaName=area.name;areaDescription=area.description||null;canDelete=role==='owner';document.getElementById('area-name').value=area.name;document.getElementById('area-type').value=area.area_type;document.getElementById('delete-area-name').textContent=area.name;document.getElementById('delete-area-confirmation-name').textContent=area.name;form.hidden=false;deleteSection.hidden=!canDelete;message.textContent='';}
form.addEventListener('submit',async e=>{e.preventDefault();const name=document.getElementById('area-name').value.trim(),type=document.getElementById('area-type').value;if(!name){message.textContent="Il nome dell'Area è obbligatorio.";return;}save.disabled=true;message.textContent='Salvataggio in corso…';const{error}=await supabaseClient.rpc('update_area',{p_area_id:areaId,p_name:name,p_area_type:type,p_description:areaDescription});if(error){message.textContent='Impossibile salvare le modifiche.';save.disabled=false;return;}back();});document.getElementById('cancel-button').addEventListener('click',back);document.getElementById('open-delete-area-dialog').addEventListener('click',()=>{if(!canDelete)return;deleteInput.value='';deleteMessage.textContent='';updateDeleteConfirmationState();deleteDialog.showModal();deleteInput.focus();});document.getElementById('close-delete-area-dialog').addEventListener('click',closeDeleteDialog);document.getElementById('cancel-delete-area').addEventListener('click',closeDeleteDialog);deleteInput.addEventListener('input',updateDeleteConfirmationState);deleteForm.addEventListener('submit',async e=>{e.preventDefault();if(!canDelete||deleteInput.value!==areaName)return;deleteConfirm.disabled=true;deleteMessage.textContent='Eliminazione in corso…';const{error}=await supabaseClient.rpc('delete_area',{p_area_id:areaId});if(error){deleteMessage.textContent='Impossibile eliminare l’Area.';updateDeleteConfirmationState();return;}location.href='mie-aree.html';});load();
