const supabaseClient=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);const form=document.getElementById('contact-form'),message=document.getElementById('message');form.addEventListener('submit',async e=>{e.preventDefault();const first=document.getElementById('first-name').value.trim(),last=document.getElementById('last-name').value.trim()||null,birth=document.getElementById('birth-date').value||null,email=document.getElementById('email').value.trim(),phone=document.getElementById('phone').value.trim();if(!first){message.textContent='Inserisci il nome.';return;}const{data:id,error}=await supabaseClient.rpc('create_contact',{p_first_name:first,p_last_name:last,p_birth_date:birth});if(error){console.error('create_contact failed',error);message.textContent='Impossibile creare il contatto. Riprova.';return;}const methods=[email&&{type:'email',value:email,is_primary:true},phone&&{type:'phone',value:phone,is_primary:!email}].filter(Boolean);if(methods.length){const{error:e}=await supabaseClient.rpc('replace_contact_methods',{p_contact_id:id,p_methods:methods});if(e){console.error('replace_contact_methods failed',e);message.textContent='Contatto creato, ma metodi non salvati.';return;}}location.href=`contatto.html?contact_id=${encodeURIComponent(id)}`;});
