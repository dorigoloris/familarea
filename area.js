const supabaseClient=supabase.createClient(SUPABASE_URL,SUPABASE_KEY),$=id=>document.getElementById(id),params=new URLSearchParams(location.search),message=$('message'),areaId=params.get('area_id'),participantsView=params.get('view')==='participants';const labels={family:'Famiglia',school:'Scuola',sport:'Sport',friends:'Amici',course:'Corso',travel:'Viaggio',other:'Area'};
function row(title,meta,href){const a=document.createElement('a');a.className='area-compact-row';a.href=href;a.innerHTML='<span class="area-compact-row-content"><strong></strong><span class="area-compact-row-meta"></span></span><span class="area-row-indicator" aria-hidden="true">&rarr;</span>';a.querySelector('strong').textContent=title;a.querySelector('.area-compact-row-meta').textContent=meta||'';return a;}
function showProgram(data){$('events-section').hidden=false;$('events-count').textContent=data.length;$('events-preview').replaceChildren(...data.map(x=>row(x.title,x.description||x.location||'',`evento.html?area_id=${encodeURIComponent(areaId)}&event_id=${encodeURIComponent(x.id)}`)));$('events-message').textContent=data.length?'':'Nessun elemento nel programma visibile.';}
async function renderParticipants(events){const people=new Map();await Promise.all(events.map(async event=>{const{data}=await supabaseClient.rpc('get_event_participants',{p_event_id:event.id});(data||[]).forEach(p=>{const current=people.get(p.profile_id)||{...p,items:[]};current.items.push(event.title);people.set(p.profile_id,current);});}));$('members-list').replaceChildren(...[...people.values()].sort((a,b)=>`${a.first_name} ${a.last_name}`.localeCompare(`${b.first_name} ${b.last_name}`,'it')).map(p=>{const li=document.createElement('li');li.className='area-participant-row';const name=document.createElement('strong');name.textContent=`${p.first_name||''} ${p.last_name||''}`.trim()||'Partecipante';const items=document.createElement('span');items.className='area-participant-role';items.textContent=[...new Set(p.items)].join(' · ');li.append(name,items);return li;}));$('participants-count').textContent=people.size;}
async function load(){const{data:s}=await supabaseClient.auth.getSession();if(!s.session){location.href='login.html';return;}if(!areaId){message.textContent='Area non specificata.';return;}if(participantsView){document.body.classList.add('area-participants-view');}$('add-program-link').href=`nuovo-evento.html?area_id=${encodeURIComponent(areaId)}`;const[{data:area,error},{data:areas},{data:events}]=await Promise.all([supabaseClient.rpc('get_area',{p_area_id:areaId}),supabaseClient.rpc('get_my_areas'),supabaseClient.rpc('get_visible_events')]);if(error||!area){message.textContent='Impossibile caricare l\'Area.';return;}const role=(areas||[]).find(x=>x.id===areaId)?.role||'member';$('area-name').textContent=area.name;$('area-type-label').textContent=labels[area.area_type]||'Area';$('area-role-badge').textContent=role==='owner'?'Proprietario':role==='admin'?'Amministratore':'Partecipante';$('area-role-meta').hidden=false;if($('edit-area-link')){$('edit-area-link').href=`modifica-area.html?area_id=${encodeURIComponent(areaId)}`;$('edit-area-link').hidden=!['owner','admin'].includes(role);}const areaEvents=(events||[]).filter(x=>x.area_id===areaId);if(participantsView){await renderParticipants(areaEvents);}else{showProgram(areaEvents);}message.textContent='';}load();
