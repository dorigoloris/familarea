const supabaseClient=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);const areaId=new URLSearchParams(location.search).get('area_id'),message=document.getElementById('message'),container=document.getElementById('lists-list');
function card(l){const a=document.createElement('a');a.className='list-card';a.href=`lista.html${l.area_id?`?area_id=${encodeURIComponent(l.area_id)}&`:'?'}list_id=${encodeURIComponent(l.id)}`;const h=document.createElement('h2');h.textContent=l.title;const p=document.createElement('p');p.textContent=l.description|| (l.area_id?'Lista Area':'Personale');a.append(h,p);return a;}
async function load(){const{data:s}=await supabaseClient.auth.getSession();if(!s.session){location.href='login.html';return;}document.getElementById('new-list-button').onclick=()=>location.href=areaId?`nuova-lista.html?area_id=${encodeURIComponent(areaId)}`:'nuova-lista.html';if(areaId){document.getElementById('back-link').href=`area.html?area_id=${encodeURIComponent(areaId)}`;}else{document.getElementById('back-link-container').hidden=true;document.getElementById('area-name').hidden=true;}const{data,error}=await supabaseClient.rpc('get_visible_lists');if(error){message.textContent='Impossibile caricare le liste.';return;}const lists=(data||[]).filter(l=>!areaId||l.area_id===areaId);container.replaceChildren(...(lists.length?lists.map(card):[Object.assign(document.createElement('p'),{className:'empty-state',textContent:areaId?'Nessuna lista in questa Area.':'Nessuna lista.'})]));message.textContent='';}load();
